<mah:MetroWindow x:Class="testapps.Views.CredentialWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:lang="clr-namespace:TaskHive"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 Title=""
                 Width="600"
                 Height="230"
                 WindowStartupLocation="CenterScreen" ResizeMode="NoResize" >

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="{x:Static lang:Strings.UserName}" Foreground="White"/>
            <TextBox x:Name="txtUsername"/>
            <TextBlock Text="{x:Static lang:Strings.password}" Margin="0,10,0,0" Foreground="White"/>
            <PasswordBox x:Name="txtPassword" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2" Margin="0,15,0,0">
            <Button Content="{x:Static lang:Strings.OK}" Width="60" Margin="5" Click="Ok_Click"/>
            <Button Content="{x:Static lang:Strings.Cancel}" Width="60" Margin="5" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>


</mah:MetroWindow>
